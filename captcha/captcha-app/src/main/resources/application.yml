server:
  port: 8080

spring:
  application:
    name: opex-captcha
  main:
    allow-circular-references: true

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: ["health", "prometheus", "metrics"]
  endpoint:
    health:
      show-details: always
